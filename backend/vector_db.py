import chromadb 
from PIL import Image
from transformers import CLIPProcessor, CLIPModel
import torch 

def crop_image(image, box): 
    #image is a PIL Image object
    # box is [x1,x2, y1, y2] in pixel coordinates
    #return the cropped image\
    pass
